[pytest]
testpaths = tests
addopts = -q
# I test non toccano il Raspberry: picamera2 e libcamera sono sostituiti
# da segnaposto in tests/conftest.py, e la cartella dei dati e' temporanea.
filterwarnings =
    ignore::DeprecationWarning
